# act-sync

Bootstrap repository for Anticaptrad reconciliation and synchronization contracts.

The first reviewed state-machine and validation baseline will be added through a feature-branch pull request. This repository is not yet a deployed production service and must never contain runtime credentials or provider tokens.
